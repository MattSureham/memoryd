# 记忆架构讨论原文

---

## 外挂记忆的建议

**我（外部建议）：** 容量本身未必是最难的部分，真正难的是：即使把整本"小说"都塞进上下文，模型还能不能在正确时刻准确地把正确细节重新激活。

长上下文里常见的问题不是只有"找不到"，还有：

- 找到了相似但错误的片段
- 最近内容压过早期内容
- 多个人物或事件被错误合并
- 模型根据故事惯性补全，而不是读取证据
- 事实检索正确，但情绪、因果和叙事位置丢失

因此目标不应该是传统 RAG 的"从一堆笔记里找 top-k 段落"，而应该是尽可能重建当前话题所处的原始局部上下文。

**三层架构：**

第一层：完整原始会话，永不摘要覆盖。每条消息原样保存，摘要和知识节点只能作为索引，不能替代原始对话。

第二层：分层情景记忆，保存完整 episode（参与者、时间顺序、角色、情绪上下文、来源 turn 范围），而不是孤立的事实。

第三层：当前工作集，构造一个小型"重新体验包"：

1. 最近 20-50 轮原始对话
2. 当前主题对应的完整历史片段
3. 相关人物的关键事件
4. 与当前表达相似的过去情绪片段
5. 一份明确的事实约束表

**降低检索精度损失的方法：**

1. 混合检索（BM25 + embedding + 实体图遍历 + 时间邻近 + 对话线程邻近）
2. 按叙事边界切块（完整问答、完整叙事、人物首次介绍、纠错、情绪转折），不按固定 token 数
3. 检索后重排，要求"证据覆盖"
4. 同时召回原文和摘要
5. 给每条记忆保存来源指针（session_id, turn_range, confidence, last_verified）
6. 纠错作为高优先级记忆，形成负约束
7. 查询扩展不能替代原查询

**1M 上下文的最佳用途：** 大窗口用作可寻址的档案室，而不是每次都把整间档案室倒在桌上。完整历史长期存储→结构化索引定位相关 episode→把相关原始对话扩展到前后若干轮→形成 20K-100K 的高相关工作集→必要时再让模型访问更大的 1M 原文。

**双轨模式：** 一条轨道保留连续性（最近对话完整放在上下文里），一条轨道负责回忆（提到旧人物时自动检索对应 episode，将原始对话片段插回上下文，先恢复叙事再继续回答）。

**最核心的一句话：** 不要把记忆压缩成事实；要把记忆索引到原始经历。知识图谱负责找到门，原始 conversation episode 才是门后的房间。

---

## 事实约束如何生成

**Matthew：** 有一个问题：你说的"当前工作集"中第 5 条"事实约束表"——这个具体怎么生成？是每次对话前手动标注，还是设想模型能从纠错历史中自动提取？

**外部建议：** 不能靠手动，也不能完全自动。应该采用"自动生成 + 人工确认"的方式。

事实约束和普通记忆不是一回事。普通记忆是"Andrew 是室友"，事实约束是"谈到 Andrew 时 NEVER call him Michael"或"Visual reasoning: DO NOT infer off-screen events"。它不是知识，是禁止模型犯某类错误。

**四种来源：**

1. 用户纠错（最高优先级）。比如"第二张图也没有第二个人"，系统自动生成约束"图片推理时永远不要从截图推断看不见的人"
2. 重复错误。连续犯三次同类错误→自动升格为约束
3. 用户明确要求。比如"分析图片的时候不要脑补"
4. 模型自己反思。回答结束后让另一个模型检查 evidence 是否支持 answer，发现不支持则生成约束

**约束的三级作用域：**

- Global：永远成立（不编造引用、不合并 session）
- User：针对特定用户（Ruby 是猫、不剧透青之箱）
- Session：对话结束自动消失（只描述图中可见内容）

**还有更厉害的方法：** 不生成 rule，而是生成 Counterexample（反例）。保存"错误声明→用户纠正→原因→教训"，以后类似任务前把过去类似错误放进 prompt。LLM 擅长"不要再犯这个错误"。

**Known Failure Modes：** 让模型自己维护一份"已知失败模式"清单，每周更新，越来越准。

**真正的问题：** 什么时候一次纠错值得被固化？不是每一句"你说错了"都应该进入长期记忆。要分成两类：

1. 事实纠错（Fact Correction）：修正世界模型，如"Ruby 是猫"
2. 行为纠错（Reasoning Constraint）：修正推理方式，如"分析图片时不要用剧情补全画面"

后者比前者更珍贵，因为它适用于所有未来类似任务。

---

## 约束也应该作为一种记忆来管理

**Matthew：** 约束本身也需要过期和合并。比如"不要合并不同 session"这个约束，如果模型连续三十次没有犯这个错误，它可能不再需要放在高频检索位置，可以降级到后台。如果某个约束被频繁触发，说明模型反复犯这类错，应该被提升到更浅的检索层。这本质上是把约束也当成"记忆"来管理——约束有自己的生命周期和优先级。

**外部建议：** 这个思路已经接近操作系统，因为它不是在讨论知识，而是在讨论调度（scheduling）。

**三种记忆：**

1. World Memory（世界模型）：回答"世界是什么样子的"，Ruby 是猫
2. Episode Memory（经历）：回答"我们一起经历了什么"，2017 年医院那晚
3. Policy Memory（策略）：回答"我应该怎么思考"，图片推理需要视觉证据

Policy 和 CPU Cache 很像：

- L1：最近频繁犯的错误，每次图片都加载
- L2：用户长期约束，如不剧透青之箱
- L3：几个月前犯过，很少触发，放后台
- Archive：连续一年没犯

**约束还需要 Confidence 和 Dependency：**

- Confidence：有些约束可能后来被推翻（用户说"默认用英文"但后来一直中文，系统应该自己 decay）
- Dependency：不是 Blue Box 讨论就不加载不剧透约束；但视觉推理约束是 Global

**Policy 能组成 Graph：**
```
Visual
├── Evidence First
├── Don't infer identity
├── Don't infer actions
└── Ask when ambiguous
```

---

## 从 Known Failure Modes 到 Self Calibration Profile

**外部建议：** 它其实不是 Failure，而是 Calibration。一个成熟的人不是什么都会，而是知道什么时候自己容易错。医生会说"这里把握只有 40%"，法官会说"证据不足"，程序员会说"这里我没读源码"。真正成熟的是知道自己的置信度。

所以不应该设计 Known Failure Modes，而应该设计 Self Calibration Profile：

```yaml
Vision:
  when: anime_screenshots
  risk: narrative_completion
  mitigation: only describe visible evidence first

Conversation:
  when: long_session
  risk: entity_merging
  mitigation: retrieve original episode
```

每次回答不是"查 Memory"，而是先查"我现在最容易犯什么错"，然后再回答。

这已经从"怎么让 AI 记住更多"变成了"怎么让 AI 越来越了解自己的认知边界"。第一条是增加容量，第二条是提升元认知（metacognition）。

**Matthew：** Calibration Profile 本身会不会退化？如果一个约束长期没有触发——比如用户三个月没发图片，"不要脑补画面"这条 Policy 会降级到 L3，置信度衰减。但如果某天用户又发了一张动漫截图，系统需要能在检索到这张图的瞬间重新激活这个约束。这个"重新激活"的触发条件是什么？向量相似度还是显式标签？

---

## Trigger：事件驱动 + 相似度辅助

**外部建议：** 都不是。应该是"事件驱动（event-driven）+ 相似度辅助（similarity-assisted）"。

为什么不是显式标签？Image→Load Visual Policy 太粗了。图片有自拍、发票、UI 截图、动漫截图、医学影像——真正需要加载"不推断不可见动作"的是动漫、影视、新闻图片，而不是支付宝截图。

为什么不是 embedding？因为 Policy 不是 Semantic。今天真正的错误不是 Blue Box，而是 Narrative Completion。Embedding 容易找到 Blue Box 而不是 Narrative Completion。

**Trigger 的设计：** 不是 Entity，不是 Episode，不是 Policy，而是 Trigger。

```yaml
Trigger:
  Condition: Visual Question + Image contains humans + User asks "这一幕"
  Activate: Visual Policy Set
```

真正触发的是 Question Intent，不是 Image。用户问"这是哪一幕"→需要视觉证据+需要剧情知识→冲突风险高→激活"不要合并证据和记忆"。

**四层系统：**
```
Memory → Policy → Trigger → Reasoning
```
Trigger 负责"什么时候"，Policy 负责"怎么做"。

**Policy 不能 decay，Trigger 的激活频率可以 decay。** Visual Policy 一直存在，但如果过去三个月没分析图片，Visual Trigger 的 Priority 越来越低。今天用户上传图片，Trigger 瞬间回到 100%，Policy 立刻加载。Decay 的不是 Policy，而是 Activation Frequency。

**Trigger 应该自己学习。** 第一次纠正→第二次又纠正→系统发现 Visual + Narrative Completion → Wrong，自动新建 Trigger，以后越来越准。

---

## Risk Recognizer：先识别风险，再检索知识

**外部建议：** 更接近人脑的模型——人不是看见图片才想起"不要脑补"，而是感觉"这里危险"。医生看到 CT，脑子不是加载 CT 知识，而是先冒出来"这个地方最容易误诊"。程序员看到多线程，第一反应是"Race Condition"。

真正触发的不是对象，而是 Risk Pattern。Calibration Profile 应该预测的不是"会不会出错"，而是"最容易出哪种错"。

**Matthew：** 这让我想到心理学里的经典区分：专家不是因为知道更多，而是因为他们更早意识到"这里需要换一种思考方式"。医生看到胸痛先判断常规还是高风险，程序员看到并发代码启动不同的检查清单。

Risk Recognizer 不是"检索条件"，而是风险识别器。它的职责不是找到知识，而是告诉系统："接下来不要用默认推理流程。切换到另一套认知模式。"

长期 AI 的核心组件可能不是 Memory Manager，而是一个一直在问自己的小模块："这一题，我应该先担心哪一种错误？"

---

## 两级 Risk Recognizer + 信息顺序控制

**外部建议：** Risk Recognizer 必须比主推理更快、更便宜。它不该承担"理解整个问题"的任务，而只负责根据当前输入特征，预测最可能发生的错误类型。更像一个前置路由器。

**第一级：确定性规则路由（几乎零成本）**

```yaml
signals:
  has_image: true
  asks_specific_visible_detail: true
output:
  risk_modes: [visual_overreach, narrative_completion]
```

保留少量"保险丝"——反复触发、高度确定的风险模式。

**第二级：轻量分类器（规则不确定时启用）**

输入压缩特征（task_type, user_intent, context_age, entities_count, prior_failure_modes），输出风险分布：

```yaml
risk_distribution:
  narrative_completion: 0.76
  visual_overinterpretation: 0.61
  entity_confusion: 0.18
```

主模型不看到内部计算，只接收模式切换指令和强度参数。

**纠错不能直接更新分类器：**

纠错事件 → 错误归因 → 生成候选风险模式 → 聚类相似错误 → 达到阈值后更新 Risk Recognizer

例如一次"Ruby 被当成人"只是偶发错误。但如果 Ruby 被当成人 + Andrew 混淆 + session 合并 + 三人错位——这些聚成 entity_resolution_under_long_context 后才值得修改前置识别器。否则系统会产生大量狭窄规则："看到 Ruby 不要当成人"，这不是校准，是记答案。

**Risk Recognizer 训练目标：** 多标签预测 P(error_type | input features, task intent, context state, past failures)，因为一个问题可能同时有多个风险。输出是风险向量，不是单一类别。

**模式切换不是开关，是强度调节：**

```yaml
mode:
  evidence_first: {level: high}
  uncertainty: {level: medium}
  retrieve_original_source: {level: high}
  ask_clarification: {level: low}
  narrative_completion_gate: {level: blocked}
```

**Memory Retrieval 放在模式控制之后：** 不同风险模式决定不同检索策略和顺序。叙事补全风险下，先独立观察画面→固化可见事实→再加载剧情记忆→比较两者，而不是同时处理。专家不仅更早进入正确模式，也知道哪些信息应该暂时隔离。

**Risk Recognizer 是一个轻量级的认知流程编排器：**

```yaml
plan:
  - inspect_primary_evidence
  - lock_observations
  - retrieve_related_episode
  - check_conflicts
  - answer_with_calibrated_confidence
```

**快慢双轨：**

- 慢速学习层（离线，大模型）：总结错误、发现新模式、更新自画像
- 快速控制层（在线，小模型/规则）：实时识别风险、切换模式
- 主推理层（在线，大模型）：完成任务

经验形成得慢，但危险识别发生得快。

---

## 完整流水线

```
用户输入
  ↓
Feature Extractor
  ↓
Risk Recognizer（两级：规则 + 分类器）
  ↓
Cognitive Mode Controller（模式 + 强度 + 检索顺序）
  ↓
Memory Retrieval（按模式决定策略）
  ↓
Main Reasoning（在活跃约束下）
  ↓
Verifier
  ↓
回答
  ↓
Correction Analyzer（归因 → 聚类 → 阈值）
  ↓
Self Calibration Profile 更新
```
