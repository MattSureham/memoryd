# 长期记忆系统架构

## 整体流水线

```
用户输入
  │
  ▼
Feature Extractor（提取输入特征）
  │
  ▼
Risk Recognizer（两级：规则路由 + 轻量分类器）
  │  输出: 风险向量 P(error_type | features, task, context, past)
  │
  ▼
Cognitive Mode Controller（模式切换 + 强度调节）
  │  输出: 认知模式参数 + 信息检索顺序
  │
  ▼
Memory Retrieval（按模式决定检索策略和顺序）
  │
  ▼
Main Reasoning（在活跃约束下运行）
  │
  ▼
Verifier（自检）
  │
  ▼
回答
  │
  ▼
Correction Analyzer（错误归因 → 聚类 → 达到阈值后更新）
  │
  ▼
Self Calibration Profile 更新
```

---

## 核心组件

### 1. Risk Recognizer — 两级架构

**第一级：确定性规则路由（几乎零成本）**

处理高置信度、稳定、可解释的信号：

```yaml
signals:
  has_image: true
  asks_specific_visible_detail: true
  contains_identity_question: false

output:
  risk_modes:
    - visual_overreach
    - narrative_completion
```

```yaml
signals:
  multiple_similar_names: true
  long_session: true
  asks_to_recall_old_person: true

output:
  risk_modes:
    - entity_merge
    - false_recall
```

规则不是手工维护全部，而是保留少量"保险丝"——反复触发、高度确定的风险模式。

**第二级：轻量分类器（规则不确定时启用）**

输入不需要完整上下文，只需要压缩特征：

```yaml
task_type: image_interpretation
user_intent: identify_scene
context_age: long
retrieved_memory_conflict: possible
entities_count: 3
prior_failure_modes:
  - narrative_completion
```

输出风险分布（多标签，不是单分类）：

```yaml
risk_distribution:
  narrative_completion: 0.76
  visual_overinterpretation: 0.61
  entity_confusion: 0.18
  spoiler_risk: 0.32
```

**Risk Recognizer 自身校准**

它不是"会不会出错"，而是"最容易出哪种错"。也不应该过度敏感导致每句话都畏缩。输出通过 Cognitive Mode Controller 转化为强度参数，而不是简单的开关。

### 2. Cognitive Mode Controller — 模式 + 强度

模式切换不是二元开关，而是多维度的强度调节：

```yaml
mode:
  evidence_first:
    level: high          # 先观察、再加载记忆
  uncertainty:
    level: medium        # 表达不确定性的程度
  retrieve_original_source:
    level: high          # 是否必须回到原始片段
  ask_clarification:
    level: low           # 是否主动追问
  narrative_completion_gate:
    level: blocked       # 剧情记忆是否允许进入
```

输出不仅是模式参数，还有**信息检索顺序**。不同模式对应不同检索策略：

| 风险模式 | 检索策略 |
|---|---|
| entity_merge | 优先精确名称 → 时间线 → 关系图 |
| narrative_completion | 先独立观察画面 → 固化可见事实 → 再加载记忆 |
| false_recall | 只允许基于原始会话片段回答 |
| spoiler_risk | 先过滤用户观看进度之后的信息 |

**核心原则：Memory Retrieval 放在模式控制之后，不是之前。** 剧情记忆不能和视觉观察同时加载，否则前者会污染后者。

### 3. 三层记忆

```
┌──────────────────────────────────────────────────┐
│              World Memory（世界模型）              │
│  回答: 世界什么样                                 │
│  Ruby: cat    六七: 初恋    PGWP: 已失去          │
│  检索: 精确匹配 + 实体图遍历                      │
├──────────────────────────────────────────────────┤
│            Episode Memory（原始经历）              │
│  回答: 我们一起经历了什么                         │
│  机场公交车 | 六七最后半年 | 雏告白               │
│  按 episode 切块，不按 token 切                   │
│  存原文片段，不存摘要                             │
│  检索: 语义相似 + 时间邻近 + 实体关联             │
├──────────────────────────────────────────────────┤
│            Policy Memory（推理策略）               │
│  回答: 我应该怎么思考                             │
│  Global:  不编造引用 | 不合并 session              │
│  User:    Ruby 永远是猫 | 不剧透青之箱             │
│  Session: 只描述图中可见 | 对话结束自动消失         │
│  约束本身也是记忆，有生命周期和依赖关系             │
└──────────────────────────────────────────────────┘
```

### 4. Self Calibration Profile — 系统自画像

不是存已知事实，而是存**认知边界**：

```yaml
Visual:
  when: anime_screenshots
  risk: narrative_completion
  mitigation: evidence_first_mode

Conversation:
  when: long_session + multiple_entities
  risk: entity_merging
  mitigation: retrieve_original_episode

Entity:
  when: similar_names + sparse_mentions
  risk: identity_confusion
  mitigation: entity_discrimination_mode
```

---

## 纠错闭环（慢速学习层）

一次纠错不能直接更新分类器。需要经过归因和聚类：

```
纠错事件
  │
  ▼
错误归因
  │  事实错？推理模式错？
  ▼
生成候选风险模式
  │
  ▼
聚类相似错误
  │  Ruby被当成人 + Andrew混淆 + session合并 + 三人错位
  │  → 聚成: entity_resolution_under_long_context
  │
  ▼
达到阈值后更新 Risk Recognizer
```

**区分两种纠错路径：**

| | 事实纠错 | 行为纠错 |
|---|---|---|
| 例子 | Ruby 是猫 | 不要用剧情补全画面 |
| 更新 | World Memory | Policy Memory + Self Calibration Profile |
| 影响 | 单个实体 | 所有未来类似任务 |
| 安全性 | 可直接更新 | 需要聚类验证后再更新分类器 |

---

## 快慢双轨

```
慢速学习层（离线，大模型）
  分析纠错 → 发现新风险模式 → 聚类 → 更新 Self Calibration Profile
  ↓ 喂养
快速控制层（在线，规则 + 轻量分类器）
  Feature Extract → Risk Recognizer → Mode Controller
  ↓ 指令
主推理层（在线，大模型）
  Memory Retrieval → Main Reasoning → Verifier → 回答
```

慢速学习层负责"经验形成"（慢但深刻），快速控制层负责"危险识别"（快但浅），主推理层负责"执行"。

---

## 关键设计决策

1. **Risk Recognizer 不是 Memory 的下游，是推理引擎的前置** — 职责是"先担心哪种错误"，不是"先检索什么知识"

2. **信息呈现顺序由风险模式决定** — 先独立观察画面 → 固化可见事实 → 再加载剧情记忆 → 比较两者。而不是同时加载

3. **模式切换是强度调节，不是开关** — evidence_first: high / uncertainty: medium / narrative_completion_gate: blocked

4. **Episode Memory 存原文，不存摘要** — 摘要只是索引（地图），原文片段是目的地（房间）。按 episode 切块，不按 token 切块

5. **Policy 不 decay，Trigger 的激活频率 decay** — Visual Policy 永远存在，只是优先级随未触发时间降低

6. **约束自己也是记忆** — Session 约束对话结束自动消失；Global 约束永久；长期未触发降级到后台

7. **一次纠错 ≠ 一次规则** — 聚类后达到阈值才更新 Risk Recognizer，否则系统会产生大量狭窄规则（"看到 Ruby 时不要当成人"），这不是校准，是记答案
